class "Generic" {
     public = {
        __construct = function(self)
        end;

        server_onCreate = function (self)
        end;

        client_onCreate = function (self)
        end;

        client_onSetupGui = function (self)
        end;

        server_onDestroy = function (self)
        end;

        server_onRefresh = function (self)
        end;

        server_onFixedUpdate = function (self, deltaTime)
        end;

        client_onFixedUpdate = function (self, deltaTime)
        end;

        client_onUpdate = function (self, deltaTime)
        end;
    }
}
